# IDS Products Portal frontend

React and TypeScript client for the IDS Products Portal.

Run npm ci, then npm run dev. Start the API separately on port 5149.
To use another API address, set VITE_API_URL in .env.local, including /api.

Checks: npm run lint and npm run build.
See the root README for backend and database setup.
